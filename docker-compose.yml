version: '3'
services: 
  app:
    image: geovanedasilva/node-deploy
    container_name: 'node-deploy'
    restart: always
    ports:
      - "3000:3000"
    environment:
      - HOST:0.0.0.0
      - PORT=3000
